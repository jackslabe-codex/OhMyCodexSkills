---
title: "question-2025-09-12"
type: question
question_id: "question-2025-09-12"
year: 2025
month: 9
no: 12
level: 5
subject: cpp
question_type: single_choice
answer: "D"
analysis: "归并排序的merge操作中，前两个while循环分别处理左右子数组未合并完的情况。第一个while处理左子数组剩余元素（条件为`i <= mid`已给出），第二个while应处理右子数组剩余元素，对应条件应为`j <= right`。"
concept_refs: ["concept-divide-and-conquer"]
difficulty: "easy"
extraction_status: ok
source: "Gesp_Cpp_2025_9.pdf"
---

## Stem

第12题 下述C++代码实现了归并排序算法，则横线上应填写（ ）。

```cpp
void merge(vector<int> &nums, int left, int mid, int right) {
    // 左子数组区间为 [left, mid], 右子数组区间为 [mid+1, right]
    vector<int> tmp(right - left + 1);
    int i = left, j = mid + 1, k = 0;
    while (i <= mid && j <= right) {
        if (nums[i] <= nums[j])
             tmp[k++] = nums[i++];
        else
             tmp[k++] = nums[j++];
    }
    while (i <= mid) {
        tmp[k++] = nums[i++];
    }
    while (________) {     // 在此处填入代码
        tmp[k++] = nums[j++];
    }
    for (k = 0; k < tmp.size(); k++) {
        nums[left + k] = tmp[k];
    }
}
```

A. `i < mid`
B. `j < right`
C. `i <= mid`
D. `j <= right`

## Answer

D

## Analysis

归并排序的merge操作中，前两个while循环分别处理左右子数组未合并完的情况。第一个while处理左子数组剩余元素（条件为`i <= mid`已给出），第二个while应处理右子数组剩余元素，对应条件应为`j <= right`。

## Links

- [[exam-2025-09]]
- [[concept-divide-and-conquer]]
