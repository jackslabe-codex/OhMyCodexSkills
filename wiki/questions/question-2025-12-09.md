---
title: "question-2025-12-09"
type: question
question_id: "question-2025-12-09"
year: 2025
month: 12
no: 9
level: 5
subject: cpp
question_type: single_choice
answer: "C"
analysis: ""
concept_refs: ["concept-complexity", "concept-cpp-high-precision", "concept-binary-search", "concept-divide-and-conquer"]
difficulty: "unknown"
extraction_status: ok
---

## Stem

第 9 题

下面代码实现了归并排序。下述关于归并排序的说法中，不正确的是（ ）。

```cpp
void merge(vector<int>& arr, vector<int>& temp, int l, int mid, int r) {
    int i = l, j = mid + 1, k = l;
    while (i <= mid && j <= r) {
        if (arr[i] <= arr[j]) temp[k++] = arr[i++];
        else temp[k++] = arr[j++];
    }
    while (i <= mid) temp[k++] = arr[i++];
    while (j <= r) temp[k++] = arr[j++];
    for (int p = l; p <= r; p++) arr[p] = temp[p];
}

void mergeSort(vector<int>& arr, vector<int>& temp, int l, int r) {
    if (l >= r) return;
    int mid = l + (r - l) / 2;
    mergeSort(arr, temp, l, mid);
    mergeSort(arr, temp, mid + 1, r);
    merge(arr, temp, l, mid, r);
}
```

A. 归并排序的平均复杂度是 $O(n\log n)$。
B. 归并排序需要 $O(n)$ 的额外空间。
C. 归并排序在最坏情况的时间复杂度是 $O(n^2)$。
D. 归并排序适合大规模数据。

## Answer

C

## Analysis

首版未生成解析。

## Links

- [[exam-2025-12]]
- [[concept-complexity]]
- [[concept-cpp-high-precision]]
- [[concept-binary-search]]
- [[concept-divide-and-conquer]]
