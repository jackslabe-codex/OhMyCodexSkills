---
title: "question-2026-03-12"
type: question
question_id: "question-2026-03-12"
year: 2026
month: 3
no: 12
level: 5
subject: cpp
question_type: single_choice
answer: "B"
analysis: "归并排序的合并过程：当A组当前元素小于等于B组当前元素时，先取A组元素（保证稳定性），否则取B组元素。题目要求从小到大排列，故条件为A[i] <= B[j]。"
concept_refs: ["concept-divide-and-conquer"]
difficulty: "easy"
extraction_status: ok
source: "Gesp_Cpp_2026_3.pdf"
---

## Stem

游戏大赛决赛，两组选手分别按得分从小到大排好队，现在要把他们合并成一个有序排行榜。

A组： A = {12, 35, 67, 89} ，B组： B = {20, 45, 55, 78} ，下面是归并合并函数的核心循环，横线处应填入（ ）。

```cpp
int i = 0, j = 0;
vector<int> result;

while (i < A.size() && j < B.size()) {
    if (___________________) {
        result.push_back(A[i++]);
    } else {
        result.push_back(B[j++]);
    }
}

while (i < A.size()) {
    result.push_back(A[i++]);
}

while (j < B.size()) {
    result.push_back(B[j++]);
}
```

A. A[i] >= B[j]
B. A[i] <= B[j]
C. i >= j
D. i <= j

## Answer

B

## Analysis

归并排序的合并过程：当A组当前元素小于等于B组当前元素时，先取A组元素（保证稳定性），否则取B组元素。题目要求从小到大排列，故条件为A[i] <= B[j]。

## Links

- [[exam-2026-03]]
- [[concept-divide-and-conquer]]
