---
title: "question-2023-12-04"
type: question
question_id: "question-2023-12-04"
year: 2023
month: 12
no: 4
level: 5
subject: cpp
question_type: single_choice
answer: "A"
analysis: "目标是将偶数排在奇数前面（稳定或非稳定均可）。当 lstA[j] 为奇数（!isEven 为真）且 lstA[j+1] 为偶数（isEven 为真）时，需要交换两者位置，使偶数前移、奇数后移。"
concept_refs: []
difficulty: "easy"
extraction_status: ok
source: "Gesp_Cpp_2023_12.pdf"
---

## Stem

下面的C++用于对 lstA 排序，使得偶数在前奇数在后，横线处应填入( )。

```cpp
bool isEven(int N)
{
    return N % 2 == 0;
}
void swap(int &a, int &b)
{
    int t;
    t=a;a=b;b=t;
    return;
}
void sortA(int lstA[], int n)
{
    int i,j,t;
    for (i = n-1; i > 0; i--)
        for(j = 0; j < i; j++)
            if(__________________________)
                swap(lstA[j], lstA[j+1]);
    return;
}
```

A. !isEven(lstA[j]) && isEven(lstA[j+1])
B. isEven(lstA[j]) && !isEven(lstA[j+1])
C. lstA[j] > lstA[j+1]
D. lstA[j] < lstA[j+1]

## Answer

A

## Analysis

目标是将偶数排在奇数前面（稳定或非稳定均可）。当 lstA[j] 为奇数（!isEven 为真）且 lstA[j+1] 为偶数（isEven 为真）时，需要交换两者位置，使偶数前移、奇数后移。

## Links

- [[exam-2023-12]]
