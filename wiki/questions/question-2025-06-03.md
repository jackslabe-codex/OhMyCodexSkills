---
title: "question-2025-06-03"
type: question
question_id: "question-2025-06-03"
year: 2025
month: 6
no: 3
level: 5
subject: cpp
question_type: single_choice
answer: "D"
analysis: "双向链表尾部插入节点需要：1. 将原尾节点的next指向新节点；2. 将新节点的prev指向原尾节点；3. 更新tail指针指向新节点。选项D完整实现了这三步。"
concept_refs: ["concept-linked-list"]
difficulty: "medium"
extraction_status: ok
source: "Gesp_Cpp_2025_6.pdf"
---

## Stem

基于上题代码正确的前提下，填入相应代码完善 append() ，用于在双向链表尾部增加新节点，横线上应填写（ ）。

```cpp
void append(int data) {
    Node* newNode = new Node{data, nullptr, nullptr};

    if (is_empty()) {
        head = tail = newNode;
    } else {
        _______________________
    }
    ++size;
}
```

A.
```cpp
tail->next = newNode;
```

B.
```cpp
newNode->prev = tail;
tail = newNode;
```

C.
```cpp
tail = newNode;
newNode->prev = tail;
tail->next = newNode;
```

D.
```cpp
tail->next = newNode;
newNode->prev = tail;
tail = newNode;
```

## Answer

D

## Analysis

双向链表尾部插入节点需要：1. 将原尾节点的next指向新节点；2. 将新节点的prev指向原尾节点；3. 更新tail指针指向新节点。选项D完整实现了这三步。

## Links

- [[exam-2025-06]]
- [[concept-linked-list]]
