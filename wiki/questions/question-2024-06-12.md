---
title: "question-2024-06-12"
type: question
question_id: "question-2024-06-12"
year: 2024
month: 6
no: 12
level: 5
subject: cpp
question_type: single_choice
answer: "A"
analysis: "高精度减法中，当某一位 a[i] < b[i] 时需要向高位借位。由于数组存储可能是低位在前（如个位、十位...），借位操作应为 a[i+1]--（将高位的值减1），然后当前位加10。"
concept_refs: ["concept-cpp-high-precision"]
difficulty: "medium"
extraction_status: ok
source: "Gesp_Cpp_2024_6.pdf"
---

## Stem

要实现一个高精度减法函数，则下面代码中加划线应该填写的代码为（ ）。

```cpp
//假设a和b均为正数，且a表示的数比b大
vector<int> minus(vector<int> a, vector<int> b) {
    vector<int> c;
    int len1 = a.size();
    int len2 = b.size();
    int i, t;

    for (i = 0; i < len2; i++) {
        if (a[i] < b[i]) { //借位
            _____________   // 在此处填入代码
            a[i] += 10;
        }
        t = a[i] - b[i];
        c.push_back(t);
    }
    for (; i < len1; i++)
        c.push_back(a[i]);

    len3 = c.size();
    while (c[len3 - 1] == 0) {//去除前导0
        c.pop_back();
        len3--;
    }
    return c;
}
```

A. a[i + 1]--;
B. a[i]--;
C. b[i + 1]--;
D. b[i]--;

## Answer

A

## Analysis

高精度减法中，当某一位 a[i] < b[i] 时需要向高位借位。由于数组存储可能是低位在前（如个位、十位...），借位操作应为 a[i+1]--（将高位的值减1），然后当前位加10。

## Links

- [[exam-2024-06]]
- [[concept-cpp-high-precision]]
