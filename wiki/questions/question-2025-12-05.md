---
title: "question-2025-12-05"
type: question
question_id: "question-2025-12-05"
year: 2025
month: 12
no: 5
level: 5
subject: cpp
question_type: single_choice
answer: "D"
analysis: ""
concept_refs: ["concept-number-theory", "concept-recursion"]
difficulty: "unknown"
extraction_status: ok
---

## Stem

第 5 题

下面代码实现了欧几里得算法。下面有关说法，错误的是（ ）。

```cpp
int gcd1(int a, int b) {
    return b == 0 ? a : gcd1(b, a % b);
}

int gcd2(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
```

A. `gcd1()` 实现为递归方式。
B. `gcd2()` 实现为迭代方式。
C. 当 $a$ 较大时，`gcd1()` 实现会多次调用自身，需要较多额外的辅助空间。
D. 当 $a$ 较大时，`gcd1()` 的实现比 `gcd2()` 执行效率更高。

## Answer

D

## Analysis

首版未生成解析。

## Links

- [[exam-2025-12]]
- [[concept-number-theory]]
- [[concept-recursion]]
