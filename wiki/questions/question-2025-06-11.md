---
title: "question-2025-06-11"
type: question
question_id: "question-2025-06-11"
year: 2025
month: 6
no: 11
level: 5
subject: cpp
question_type: single_choice
answer: "A"
analysis: "该二分查找算法用于查找目标值的最后一次出现位置。当存在重复target时，由于mid计算采用向上取整`(low+high+1)/2`且条件为`lst[mid]<=target`时`low=mid`，可以确保最终low收敛到最后一个target的位置。当target小于所有元素时，最终low仍为0，但`lst[0]!=target`故返回-1而非0。若改为`while(low<=high)`可能导致死循环或逻辑错误。将mid改为`(low+high)/2`（向下取整）在`low=high-1`时会导致mid=low无法推进，造成死循环。"
concept_refs: ["concept-binary-search"]
difficulty: "hard"
extraction_status: ok
source: "Gesp_Cpp_2025_6.pdf"
---

## Stem

下面的C++代码用于在升序数组 lst 中查找目标值 target 最后一次出现的位置。相关说法，正确的是（ ）。

```cpp
int binary_search_last_occurrence(const vector<int>& lst, int target) {
    if (lst.empty()) return -1;

    int low = 0, high = lst.size() - 1;

    while (low < high) {
        int mid = (low + high + 1) / 2;
        if (lst[mid] <= target) {
            low = mid;
        } else {
            high = mid - 1;
        }
    }

    if (lst[low] == target)
         return low;
    else
         return -1;
}
```

A. 当 lst 中存在重复的 target 时，该函数总能返回最后一个 target 的位置，即便 lst 全由相同元素组成
B. 当 target 小于 lst 中所有元素时，该函数会返回 0
C. 循环条件改为 while (low <= high) 程序执行效果相同，且能提高准确性
D. 将代码中 (low + high + 1) / 2 修改为 (low + high) / 2 效果相同

## Answer

A

## Analysis

该二分查找算法用于查找目标值的最后一次出现位置。当存在重复target时，由于mid计算采用向上取整`(low+high+1)/2`且条件为`lst[mid]<=target`时`low=mid`，可以确保最终low收敛到最后一个target的位置。当target小于所有元素时，最终low仍为0，但`lst[0]!=target`故返回-1而非0。若改为`while(low<=high)`可能导致死循环或逻辑错误。将mid改为`(low+high)/2`（向下取整）在`low=high-1`时会导致mid=low无法推进，造成死循环。

## Links

- [[exam-2025-06]]
- [[concept-binary-search]]
