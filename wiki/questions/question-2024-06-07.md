---
title: "question-2024-06-07"
type: question
question_id: "question-2024-06-07"
year: 2024
month: 6
no: 7
level: 5
subject: cpp
question_type: single_choice
answer: "A"
analysis: ""
concept_refs: ["concept-number-theory", "concept-cpp-high-precision"]
difficulty: "unknown"
extraction_status: ok
---

## Stem

第 7 题

下述代码实现素数表的线性筛法，筛选出所有小于等于 `n` 的素数，则横线上应填的代码是（ ）。

```cpp
vector<int> linear_sieve(int n) {
    vector<bool> is_prime(n + 1, true);
    vector<int> primes;
    is_prime[0] = is_prime[1] = 0; // 0和1两个数特殊处理
    for (int i = 2; i <= n; ++i) {
        if (is_prime[i]) {
            primes.push_back(i);
        }
        ________________________________ { // 在此处填入代码
            is_prime[i * primes[j]] = 0;
            if (i % primes[j] == 0)
                break;
        }
    }
    return primes;
}
```

A. `for (int j = 0; j < primes.size() && i * primes[j] <= n; j++)`
B. `for (int j = 0; j <= sqrt(n) && i * primes[j] <= n; j++)`
C. `for (int j = 0; j <= n; j++)`
D. `for (int j = 1; j <= sqrt(n); j++)`

## Answer

A

## Analysis

首版未生成解析。

## Links

- [[exam-2024-06]]
- [[concept-number-theory]]
- [[concept-cpp-high-precision]]
