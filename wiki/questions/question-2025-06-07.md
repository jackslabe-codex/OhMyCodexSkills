---
title: "question-2025-06-07"
type: question
question_id: "question-2025-06-07"
year: 2025
month: 6
no: 7
level: 5
subject: cpp
question_type: single_choice
answer: "D"
analysis: "埃氏筛和线性筛都是用于批量筛选质数的算法，时间复杂度分别为O(n log log n)和O(n)，远优于对每个数单独调用试除法（O(n√n)）。但线性筛（欧拉筛）比埃氏筛效率更高（后者对合数有重复标记），因此选项D说\"埃氏筛比上面代码以及线性筛效率都高\"是错误的——它比试除法高，但不如线性筛。选项C指出试除法对连续数筛选效率低也是正确的。"
concept_refs: ["concept-number-theory", "concept-complexity"]
difficulty: "medium"
extraction_status: ok
source: "Gesp_Cpp_2025_6.pdf"
---

## Stem

下面的代码用于判断整数 n 是否是质数，错误的说法是（ ）。

```cpp
bool is_prime(int n) {
    if (n <= 1) return false;

    int finish_number = static_cast<int>(sqrt(n)) + 1;
    for (int i = 2; i < finish_number; ++i) {
        if (n % i == 0)
            return false;
    }
    return true;
}
```

A. 埃氏筛算法相对于上面的代码效率更高
B. 线性筛算法相对于上面的代码效率更高
C. 上面的代码有很多重复计算，因为不是判断单个数是否为质数，故而导致筛选出连续数中质数的效率不高
D. 相对而言，埃氏筛算法比上面代码以及线性筛算法效率都高

## Answer

D

## Analysis

埃氏筛和线性筛都是用于批量筛选质数的算法，时间复杂度分别为O(n log log n)和O(n)，远优于对每个数单独调用试除法（O(n√n)）。但线性筛（欧拉筛）比埃氏筛效率更高（后者对合数有重复标记），因此选项D说"埃氏筛比上面代码以及线性筛效率都高"是错误的——它比试除法高，但不如线性筛。选项C指出试除法对连续数筛选效率低也是正确的。

## Links

- [[exam-2025-06]]
- [[concept-number-theory]]
- [[concept-complexity]]
