---
title: "question-2025-06-14"
type: question
question_id: "question-2025-06-14"
year: 2025
month: 6
no: 14
level: 5
subject: cpp
question_type: single_choice
answer: "D"
analysis: "快速排序是不稳定排序算法。在分区过程中，元素的相对顺序可能改变（如相等的元素可能被交换到基准值的不同侧）。选项A中变量i记录的是小于等于基准值的元素的最后边界（即已处理区域的末尾）；随机选择基准值可有效避免最坏情况O(n²)，平均时间复杂度为O(n log n)。"
concept_refs: []
difficulty: "medium"
extraction_status: ok
source: "Gesp_Cpp_2025_6.pdf"
---

## Stem

关于下述C++代码的快速排序算法，说法错误的是（ ）。

```cpp
int randomPartition(std::vector<int>& arr, int low, int high) {
    int random = low + rand() % (high - low + 1);
    std::swap(arr[random], arr[high]);

    int pivot = arr[high];
    int i = low - 1;

    for (int j = low; j < high; j++) {
        if (arr[j] <= pivot) {
            i++;
            std::swap(arr[i], arr[j]);
        }
    }
    std::swap(arr[i + 1], arr[high]);
    return i + 1;
}

void quickSort(std::vector<int>& arr, int low, int high) {
    if (low < high) {
        int pi = randomPartition(arr, low, high);

        quickSort(arr, low, pi - 1);
        quickSort(arr, pi + 1, high);
    }
}
```

A. 在 randomPartition 函数中，变量 i 的作用是记录大于基准值的元素的边界
B. randomPartition 函数随机选择基准值，可以避免输入数据特定模式导致的最坏情况下时间复杂度O(n²)
C. 快速排序平均时间复杂度是O(n log n)
D. 快速排序是稳定排序算法

## Answer

D

## Analysis

快速排序是不稳定排序算法。在分区过程中，元素的相对顺序可能改变（如相等的元素可能被交换到基准值的不同侧）。选项A中变量i记录的是小于等于基准值的元素的最后边界（即已处理区域的末尾）；随机选择基准值可有效避免最坏情况O(n²)，平均时间复杂度为O(n log n)。

## Links

- [[exam-2025-06]]
