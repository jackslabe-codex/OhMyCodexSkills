---
title: "question-2024-03-07"
type: question
question_id: "question-2024-03-07"
year: 2024
month: 3
no: 7
level: 5
subject: cpp
question_type: single_choice
answer: "A"
analysis: ""
concept_refs: ["concept-cpp-high-precision"]
difficulty: "unknown"
extraction_status: ok
---

## Stem

第 7 题

下面的代码片段用于将两个高精度整数进行相加。请在横线处填入（ ），使其能正确实现相应功能。

```cpp
string add(string num1, string num2) {
    string result;
    int carry = 0;
    int i = num1.size() - 1, j = num2.size() - 1;
    while (i >= 0 || j >= 0 || carry) {
        int x = (i >= 0) ? num1[i--] - '0' : 0;
        int y = (j >= 0) ? num2[j--] - '0' : 0;
        int sum = x + y + carry;
        carry = sum / 10;
        _______________________________________
    }
    return result;
}
```

A. `result = to_string(sum % 10) + result;`
B. `result = to_string(carry % 10) + result;`
C. `result = to_string(sum / 10) + result;`
D. `result = to_string(sum % 10 + carry) + result;`

## Answer

A

## Analysis

首版未生成解析。

## Links

- [[exam-2024-03]]
- [[concept-cpp-high-precision]]
