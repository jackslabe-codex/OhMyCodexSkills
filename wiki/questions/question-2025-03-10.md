---
title: "question-2025-03-10"
type: question
question_id: "question-2025-03-10"
year: 2025
month: 3
no: 10
level: 5
subject: cpp
question_type: single_choice
answer: "B"
analysis: ""
concept_refs: ["concept-cpp-high-precision", "concept-divide-and-conquer"]
difficulty: "unknown"
extraction_status: ok
---

## Stem

第 10 题

考虑以下 C++ 代码实现的快速排序算法，将数据从小到大排序，则横线上应填的最佳代码是（ ）。

```cpp
int partition(vector<int>& arr, int low, int high) {
    int pivot = arr[high]; // 基准值
    int i = low - 1;

    for (int j = low; j < high; j++) {
        ________________________________ // 在此处填入代码
    }
    swap(arr[i + 1], arr[high]);
    return i + 1;
}

// 快速排序
void quickSort(vector<int>& arr, int low, int high) {
    if (low < high) {
        int pi = partition(arr, low, high);
        quickSort(arr, low, pi - 1);
        quickSort(arr, pi + 1, high);
    }
}
```

A.

```cpp
if (arr[j] > pivot) {
    i++;
    swap(arr[i], arr[j]);
}
```

B.

```cpp
if (arr[j] < pivot) {
    i++;
    swap(arr[i], arr[j]);
}
```

C.

```cpp
if (arr[j] < pivot) {
    swap(arr[i], arr[j]);
    i++;
}
```

D.

```cpp
if (arr[j] == pivot) {
    i++;
    swap(arr[i], arr[j]);
}
```

## Answer

B

## Analysis

首版未生成解析。

## Links

- [[exam-2025-03]]
- [[concept-cpp-high-precision]]
- [[concept-divide-and-conquer]]
