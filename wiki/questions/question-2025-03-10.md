---
title: "question-2025-03-10"
type: question
question_id: "question-2025-03-10"
year: 2025
month: 3
no: 10
level: 5
subject: cpp
question_type: single_choice
answer: "B"
analysis: "该快速排序实现以 `arr[high]` 为基准值（pivot），`i` 指向小于 pivot 的最后一个位置。当遍历到比 pivot 小的元素时，应将其交换到左侧区域，即先 `i++`，再交换 `arr[i]` 和 `arr[j]`。"
concept_refs: ["concept-divide-and-conquer"]
difficulty: "medium"
extraction_status: ok
source: "Gesp_Cpp_2025_3.pdf"
---

## Stem

第10题 考虑以下C++代码实现的快速排序算法，将数据从小到大排序，则横线上应填的最佳代码是( )。

```cpp
int partition(vector<int>& arr, int low, int high) {
    int pivot = arr[high]; // 基准值
    int i = low - 1;

    for (int j = low; j < high; j++) {
        ________________________________           // 在此处填入代码
    }
    swap(arr[i + 1], arr[high]);
    return i + 1;
}
```

A.
```cpp
if (arr[j] > pivot) {
    i++;
    swap(arr[i], arr[j]);
}
```

B.
```cpp
if (arr[j] < pivot) {
    i++;
    swap(arr[i], arr[j]);
}
```

C.
```cpp
if (arr[j] < pivot) {
    swap(arr[i], arr[j]);
    i++;
}
```

D.
```cpp
if (arr[j] == pivot) {
    i++;
    swap(arr[i], arr[j]);
}
```

## Answer

B

## Analysis

该快速排序实现以 `arr[high]` 为基准值（pivot），`i` 指向小于 pivot 的最后一个位置。当遍历到比 pivot 小的元素时，应将其交换到左侧区域，即先 `i++`，再交换 `arr[i]` 和 `arr[j]`。

## Links

- [[exam-2025-03]]
- [[concept-divide-and-conquer]]
