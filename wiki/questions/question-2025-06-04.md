---
title: "question-2025-06-04"
type: question
question_id: "question-2025-06-04"
year: 2025
month: 6
no: 4
level: 5
subject: cpp
question_type: single_choice
answer: "A"
analysis: ""
concept_refs: ["concept-linked-list-advanced"]
difficulty: "unknown"
content_status: ok
classification_status: ok
classification_source: "deepseek_direct"
classification_reason: "使用循环链表解决约瑟夫问题，涉及循环链表操作。"
classification_confidence: 0.95
fallback_parent_used: false
classification_review_notes: ""
extraction_status: ok
---

## Stem

第 4 题

下列 C++ 代码用循环链表解决约瑟夫问题，即假设 `n` 个人围成一圈，从第一个人开始数，每次数到第 `k` 个的人就出圈，输出最后留下的那个人的编号。横线上应填写（ ）。

```cpp
struct Node {
    int data;
    Node* next;
};

Node* createCircularList(int n) {
    Node* head = new Node{1, nullptr};
    Node* prev = head;
    for (int i = 2; i <= n; ++i) {
        Node* node = new Node{i, nullptr};
        prev->next = node;
        prev = node;
    }
    prev->next = head;
    return head;
}

int fingLastSurvival(int n, int k) {
    Node* head = createCircularList(n);
    Node* p = head;
    Node* prev = nullptr;
    while (p->next != p) {
        for (int count = 1; count < k; ++count) {
            prev = p;
            p = p->next;
        }
        _______________________
    }
    cout << "最后留下的人编号是: " << p->data << endl;
    delete p;
    return 0;
}
```

A.
```cpp
prev->next = p->next;
delete p;
p = prev->next;
```

B.
```cpp
delete p;
prev->next = p->next;
p = prev->next;
```

C.
```cpp
delete p;
p = prev->next;
prev->next = p->next;
```

D.
```cpp
prev->next = p->next;
p = prev->next;
delete p;
```

## Answer

A

## Analysis

首版未生成解析。

## Links

- [[exam-2025-06]]
- [[concept-linked-list-advanced]]
