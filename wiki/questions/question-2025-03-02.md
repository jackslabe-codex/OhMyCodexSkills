---
title: "question-2025-03-02"
type: question
question_id: "question-2025-03-02"
year: 2025
month: 3
no: 2
level: 5
subject: cpp
question_type: single_choice
answer: "A"
analysis: "删除双向链表中的结点p时，需要将p的前驱结点的next指针指向p的后继结点，同时将p的后继结点的prev指针指向p的前驱结点。正确操作应为：`p->prev->next = p->next; p->next->prev = p->prev; delete p;`。选项A中`p->next->prev = p->next`将后继结点的前驱指向了自身（或错误位置），导致链表断裂，是错误的。"
concept_refs: ["concept-linked-list"]
difficulty: "medium"
extraction_status: ok
source: "Gesp_Cpp_2025_3.pdf"
---

## Stem

第2题 双向链表中每个结点有两个指针域 prev 和 next ，分别指向该结点的前驱及后继结点。设 p 指向链表中的一个结点，它的前驱结点和后继结点均非空。要删除结点 p ，则下述语句中错误的是（ ）。

A.
```cpp
p->next->prev = p->next;
p->prev->next = p->prev;
delete p;
```

B.
```cpp
p->prev->next = p->next;
p->next->prev = p->prev;
delete p;
```

C.
```cpp
p->next->prev = p->prev;
p->next->prev->next = p->next;
delete p;
```

D.
```cpp
p->prev->next = p->next;
p->prev->next->prev = p->prev;
delete p;
```

## Answer

A

## Analysis

删除双向链表中的结点p时，需要将p的前驱结点的next指针指向p的后继结点，同时将p的后继结点的prev指针指向p的前驱结点。正确操作应为：`p->prev->next = p->next; p->next->prev = p->prev; delete p;`。选项A中`p->next->prev = p->next`将后继结点的前驱指向了自身（或错误位置），导致链表断裂，是错误的。

## Links

- [[exam-2025-03]]
- [[concept-linked-list]]
