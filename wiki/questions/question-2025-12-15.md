---
title: "question-2025-12-15"
type: question
question_id: "question-2025-12-15"
year: 2025
month: 12
no: 15
level: 5
subject: cpp
question_type: single_choice
answer: "B"
analysis: ""
concept_refs: ["concept-high-precision-add-sub"]
difficulty: "unknown"
content_status: ok
classification_status: ok
classification_source: "zhipu_direct"
classification_reason: "题目考察使用数组模拟大整数的高精度加法实现，处理进位。"
classification_confidence: 1.00
fallback_parent_used: false
classification_review_notes: ""
extraction_status: ok
---

## Stem

第 15 题

下面代码实现了对两个数组表示的正整数的高精度加法（数组低位在前），则横线上应填写（ ）。

```cpp
vector<int> add(vector<int> a, vector<int> b) {
    vector<int> c;
    int carry = 0;
    for (int i = 0; i < a.size() || i < b.size(); i++) {
        if (i < a.size()) carry += a[i];
        if (i < b.size()) carry += b[i];
        _______________________ //在此处填入代码
    }
    if (carry) c.push_back(carry);
    return c;
}
```

A.

```cpp
c.push_back(carry / 10);
carry %= 10;
```

B.

```cpp
c.push_back(carry % 10);
carry /= 10;
```

C.

```cpp
c.push_back(carry % 10);
```

D.

```cpp
c.push_back(carry);
carry /= 10;
```

## Answer

B

## Analysis

首版未生成解析。

## Links

- [[exam-2025-12]]
- [[concept-high-precision-add-sub]]
