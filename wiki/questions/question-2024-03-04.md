---
title: "question-2024-03-04"
type: question
question_id: "question-2024-03-04"
year: 2024
month: 3
no: 4
level: 5
subject: cpp
question_type: single_choice
answer: "B"
analysis: ""
concept_refs: ["concept-linked-list-advanced"]
difficulty: "unknown"
content_status: ok
classification_status: ok
classification_source: "deepseek_direct"
classification_reason: "题目涉及双向链表节点的删除操作。"
classification_confidence: 1.00
fallback_parent_used: false
classification_review_notes: ""
extraction_status: ok
---

## Stem

第 4 题

下面的代码片段用于在双向链表中删除一个节点。请在横线处填入（ ），使其能正确实现相应功能。

```cpp
void deleteNode(DoublyListNode*& head, int value) {
    DoublyListNode* current = head;
    while (current != nullptr && current->val != value) {
        current = current->next;
    }
    if (current != nullptr) {
        if (current->prev != nullptr) {
            ____________________________________ // 在此处填入代码
        } else {
            head = current->next;
        }
        if (current->next != nullptr) {
            current->next->prev = current->prev;
        }
        delete current;
    }
}
```

A. `if (current->next != nullptr) current->next->prev = current->prev;`
B. `current->prev->next = current->next;`
C. `delete current->next;`
D. `current->prev = current->next;`

## Answer

B

## Analysis

首版未生成解析。

## Links

- [[exam-2024-03]]
- [[concept-linked-list-advanced]]
