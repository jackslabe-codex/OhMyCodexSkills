---
title: "question-2024-06-01"
type: question
question_id: "question-2024-06-01"
year: 2024
month: 6
no: 1
level: 5
subject: cpp
question_type: single_choice
answer: "C"
analysis: "该代码使用循环结构（for循环）和变量迭代（a, b, next的更新）来计算斐波那契数列，属于迭代算法。递归算法会使用函数自身调用自身的方式实现。"
concept_refs: ["concept-recursion"]
difficulty: "easy"
extraction_status: ok
source: "Gesp_Cpp_2024_6.pdf"
---

## Stem

下面C++代码用于求斐波那契数列，该数列第1、2项为1，以后各项均是前两项之和。函数fibo()属于( )。

```cpp
int fibo(int n) {
    if (n <= 0)
        return 0;
    if (n == 1 || n == 2)
        return 1;

    int a = 1, b = 1, next;
    for (int i = 3; i <= n; i++) {
        next = a + b;
        a = b;
        b = next;
    }
    return next;
}
```

A. 枚举算法
B. 贪心算法
C. 迭代算法
D. 递归算法

## Answer

C

## Analysis

该代码使用循环结构（for循环）和变量迭代（a, b, next的更新）来计算斐波那契数列，属于迭代算法。递归算法会使用函数自身调用自身的方式实现。

## Links

- [[exam-2024-06]]
- [[concept-recursion]]
