---
title: "question-2025-09-15"
type: question
question_id: "question-2025-09-15"
year: 2025
month: 9
no: 15
level: 5
subject: cpp
question_type: single_choice
answer: "A"
analysis: "高精度加一操作中，当遇到数字9时，需要将其置为0并向高位进位。如果所有位都是9，循环结束后会在最高位插入1。因此横线处应填写`digits[i] = 0;`。"
concept_refs: ["concept-cpp-high-precision"]
difficulty: "easy"
extraction_status: ok
source: "Gesp_Cpp_2025_9.pdf"
---

## Stem

第15题 给定一个由非负整数组成的数组digits，表示一个非负整数的各位数字，其中最高位在数组首位，且digits不含前导0（除非是0本身）。下面代码对该整数执行+1操作，并返回结果数组，则横线上应填写（ ）。

```cpp
vector<int> plusOne(vector<int>& digits) {
    for (int i = (int)digits.size() - 1; i >= 0; --i) {
        if (digits[i] < 9) {
            digits[i] += 1;
            return digits;
        }
        ________________    // 在此处填入代码
    }
    digits.insert(digits.begin(), 1);
    return digits;
}
```

A. `digits[i] = 0;`

## Answer

A

## Analysis

高精度加一操作中，当遇到数字9时，需要将其置为0并向高位进位。如果所有位都是9，循环结束后会在最高位插入1。因此横线处应填写`digits[i] = 0;`。

## Links

- [[exam-2025-09]]
- [[concept-cpp-high-precision]]
