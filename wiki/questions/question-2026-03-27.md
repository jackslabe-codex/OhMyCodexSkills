---
title: "question-2026-03-27"
type: question
question_id: "question-2026-03-27"
year: 2026
month: 3
no: 27
level: 5
subject: cpp
question_type: programming
answer: ""
analysis: ""
concept_refs: ["concept-binary-search", "concept-complexity"]
difficulty: "unknown"
extraction_status: ok
source: "Gesp_Cpp_2026_3.pdf"
---

## Stem

## 3.2 编程题 2

- **试题名称**: 找数
- **时间限制**: 1.0 s
- **内存限制**: 512.0 MB

### 3.2.1 题目描述
给定一个包含 $n$ 个互不相同的正整数的数组 $A$ 与一个包含 $m$ 个互不相同的正整数的数组 $B$，请你帮忙计算有多少数在数组 $A$ 与数组 $B$ 中均出现。

### 3.2.2 输入格式
第一行包含两个整数 $n, m$。

第二行包含 $n$ 个正整数 $a_1, a_2, \cdots, a_n$ 表示数组 $A$。

第三行包含 $m$ 个正整数 $b_1, b_2, \cdots, b_m$ 表示数组 $B$。

### 3.2.3 输出格式
输出一个整数，表示在数组 $A$ 与数组 $B$ 中均出现的数的个数。

### 3.2.4 样例
#### 3.2.4.1 输入样例
```text
3 5
4 2 3
3 1 5 4 6
```

#### 3.2.4.2 输出样例
```text
```

### 3.2.5 样例解释
样例 1 中，4、3 在数组 $A$ 与 $B$ 中均出现。

### 3.2.6 数据范围
对于 $40\%$ 的数据，保证 $1 \le n, m \le 1000$。

对于 $100\%$ 的数据，保证 $1 \le n, m \le 10^5$，$1 \le a_i, b_i \le 10^6$。

### 3.2.7 参考程序
```cpp
#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
    int n, m, l, r, mid;
    bool ok;
    cin >> n >> m;

    vector<int> a(n);
    for(int i = 0; i < n; i++)
        cin >> a[i];
    sort(a.begin(), a.end());

    int ans = 0;
    for(int i = 0, b; i < m; i++) {
        cin >> b;
        ok = false;
        l = 0;
        r = n-1;
        while(l <= r)
        {
            mid = l + (r-l)/2;
            if(a[mid] > b) r = mid - 1;
            else if(a[mid] < b) l = mid + 1;
            else
            {
                ok = true;
                break;
            }
        }
        if(ok) ans++;
    }
    cout << ans;
    return 0;
}
```

## Answer

未可靠提取。

## Analysis

首版未生成解析。

## Links

- [[exam-2026-03]]
- [[concept-binary-search]]
- [[concept-complexity]]
