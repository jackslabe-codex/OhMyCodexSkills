---
title: "question-2025-09-13"
type: question
question_id: "question-2025-09-13"
year: 2025
month: 9
no: 13
level: 5
subject: cpp
question_type: single_choice
answer: "B"
analysis: "这是经典的区间调度问题（活动选择问题）。贪心策略是按结束时间升序排序，因此排序比较函数应为`a[1] < b[1]`；在选择电影后，需要更新最后结束时间为当前电影的结束时间，即`lastEnd = movies[i][1]`。"
concept_refs: ["concept-greedy"]
difficulty: "medium"
extraction_status: ok
source: "Gesp_Cpp_2025_9.pdf"
---

## Stem

第13题 假设你是一家电影院的排片经理，只有一个放映厅。你有一个电影列表movies，其中movies[i] = [start_i, end_i]表示第i部电影的开始和结束时间。请你找出最多能安排多少部不重叠的电影，则横线上应分别填写的代码为（ ）。

```cpp
int maxMovies(vector<vector<int>>& movies) {
    if (movies.empty()) return 0;

    sort(movies.begin(), movies.end(), [](const vector<int>& a, const vector<int>& b) {
        return ______; // 在此处填入代码
    });

    int count = 1;
    int lastEnd = movies[0][1];

    for (int i = 1; i < movies.size(); i++) {
        if (movies[i][0] >= lastEnd) {
            count++;
            ______ = movies[i][1]; // 在此处填入代码
        }
    }

    return count;
}
```

A. `a[0] < b[0]` 和 `lastEnd`
B. `a[1] < b[1]` 和 `lastEnd`
C. `a[0] < b[0]` 和 `movies[i][0]`
D. `a[1] < b[1]` 和 `movies[i][0]`

## Answer

B

## Analysis

这是经典的区间调度问题（活动选择问题）。贪心策略是按结束时间升序排序，因此排序比较函数应为`a[1] < b[1]`；在选择电影后，需要更新最后结束时间为当前电影的结束时间，即`lastEnd = movies[i][1]`。

## Links

- [[exam-2025-09]]
- [[concept-greedy]]
