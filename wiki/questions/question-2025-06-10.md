---
title: "question-2025-06-10"
type: question
question_id: "question-2025-06-10"
year: 2025
month: 6
no: 10
level: 5
subject: cpp
question_type: single_choice
answer: "D"
analysis: "本题采用迭代（循环）方式遍历数组求最大值，时间复杂度为O(n)。由于没有使用递归，因此不存在递归调用栈的创建和销毁开销。但空间复杂度方面：本题只使用了常数个额外变量（max_value等），空间复杂度为O(1)；而上一题（第9题）的递归实现需要递归调用栈，空间复杂度为O(log n)（因分治深度为log n）。两者空间复杂度不相同。"
concept_refs: ["concept-complexity"]
difficulty: "medium"
extraction_status: ok
source: "Gesp_Cpp_2025_6.pdf"
---

## Stem

下面的C++代码，用于求一系列数据中的最大值。有关其算法说法错误的是（ ）。

```cpp
int find_max(const vector<int>& nums) {
    if (nums.empty()) {
        throw invalid_argument("输入数组不能为空");
    }

    int max_value = nums[0];
    for (int num : nums) {
        if (num > max_value) {
            max_value = num;
        }
    }
    return max_value;
}
```

A. 本题 find_max() 函数采用的是迭代算法
B. 本题 find_max() 函数的时间复杂度为O(n)
C. 和上一题的 find_max() 相比，因为没有递归，所以没有栈的创建和销毁开销
D. 本题 find_max() 函数和上一题的 find_max() 空间复杂度相同

## Answer

D

## Analysis

本题采用迭代（循环）方式遍历数组求最大值，时间复杂度为O(n)。由于没有使用递归，因此不存在递归调用栈的创建和销毁开销。但空间复杂度方面：本题只使用了常数个额外变量（max_value等），空间复杂度为O(1)；而上一题（第9题）的递归实现需要递归调用栈，空间复杂度为O(log n)（因分治深度为log n）。两者空间复杂度不相同。

## Links

- [[exam-2025-06]]
- [[concept-complexity]]
