---
title: "question-2026-03-02"
type: question
question_id: "question-2026-03-02"
year: 2026
month: 3
no: 2
level: 5
subject: cpp
question_type: single_choice
answer: "C"
analysis: "在双向循环链表中p之前插入s，需要：1) s->next = p; 2) s->prev = p->prev; 3) p->prev->next = s; 4) p->prev = s。这四步操作保证了s正确插入到p的前面，并维护了双向链表的指针关系。"
concept_refs: ["concept-linked-list"]
difficulty: "medium"
extraction_status: ok
source: "Gesp_Cpp_2026_3.pdf"
---

## Stem

双向循环链表中要在结点 p 之前插入新结点 s（均非空），以下指针操作正确的是（ ）。

A.
```cpp
s -> next = p;
p -> prev = s;
p -> next = s;
s -> prev = p;
```

B.
```cpp
s -> prev = p;
s -> next = p -> next;
p -> next -> prev = s;
p -> next = s;
```

C.
```cpp
s -> next = p;
s -> prev = p->prev;
p -> prev -> next = s;
p -> prev = s;
```

D.
```cpp
s -> next = p;
s -> prev = nullptr;
p -> prev = s;
```

## Answer

C

## Analysis

在双向循环链表中p之前插入s，需要：1) s->next = p; 2) s->prev = p->prev; 3) p->prev->next = s; 4) p->prev = s。这四步操作保证了s正确插入到p的前面，并维护了双向链表的指针关系。

## Links

- [[exam-2026-03]]
- [[concept-linked-list]]
