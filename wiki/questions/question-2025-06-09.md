---
title: "question-2025-06-09"
type: question
question_id: "question-2025-06-09"
year: 2025
month: 6
no: 9
level: 5
subject: cpp
question_type: single_choice
answer: "C"
analysis: "该算法通过递归将数组分为左右两部分分别求最大值，再取两者中的较大者，这是典型的分治（Divide and Conquer）思想。同时函数通过调用自身实现，属于递归实现。贪心算法通常指在每一步选择局部最优解以期望达到全局最优，本题求最大值的过程不涉及贪心策略。递推算法通常指从已知条件出发通过循环或公式逐步推导结果，与本题的递归分治不同。"
concept_refs: ["concept-divide-and-conquer", "concept-recursion"]
difficulty: "easy"
extraction_status: ok
source: "Gesp_Cpp_2025_6.pdf"
---

## Stem

下面的C++代码，用于求一系列数据中的最大值。有关其算法说法错误的是（ ）。

```cpp
int find_max_recursive(const vector<int>& nums, int left, int right) {
    if (left == right)
        return nums[left];

    int mid = left + (right - left) / 2;
    int left_max = find_max_recursive(nums, left, mid);
    int right_max = find_max_recursive(nums, mid + 1, right);

    return max(left_max, right_max);
}

int find_max(const vector<int>& nums) {
    if (nums.empty()) {
        throw invalid_argument("输入数组不能为空");
    }
    return find_max_recursive(nums, 0, nums.size() - 1);
}
```

A. 该算法采用分治算法
B. 该算法是递归实现
C. 该算法采用贪心算法
D. 该算法不是递推算法

## Answer

C

## Analysis

该算法通过递归将数组分为左右两部分分别求最大值，再取两者中的较大者，这是典型的分治（Divide and Conquer）思想。同时函数通过调用自身实现，属于递归实现。贪心算法通常指在每一步选择局部最优解以期望达到全局最优，本题求最大值的过程不涉及贪心策略。递推算法通常指从已知条件出发通过循环或公式逐步推导结果，与本题的递归分治不同。

## Links

- [[exam-2025-06]]
- [[concept-divide-and-conquer]]
- [[concept-recursion]]
