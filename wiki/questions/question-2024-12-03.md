---
title: "question-2024-12-03"
type: question
question_id: "question-2024-12-03"
year: 2024
month: 12
no: 3
level: 5
subject: cpp
question_type: single_choice
answer: "A"
analysis: "使用虚拟头节点（dummyHead）的目的是统一处理删除头节点和其他节点的逻辑。应将 dummyHead 的 next 指向原链表头 head，并将当前指针 cur 初始化为 dummyHead。这样循环中始终通过 cur->next 来判断和操作待删除节点。"
concept_refs: ["concept-linked-list"]
difficulty: "medium"
extraction_status: ok
source: "Gesp_Cpp_2024_12.pdf"
---

## Stem

为了方便链表的增删操作，一些算法生成一个虚拟头节点，方便统一删除头节点和其他节点。下面代码实现了删除链表中值为 val 的节点，横线上应填的最佳代码是( )。

```cpp
struct LinkedNode {
    int val;
    LinkedNode* next;
    LinkedNode(int val):val(val), next(nullptr){}
};

void removeElements(LinkedNode* head, int val) {
    if (head == nullptr) {
        return;
    }
    LinkedNode* cur;
    LinkedNode* dummyHead = new LinkedNode(0); //虚拟头节点
    ________________________________     // 在此处填入代码

    while(cur ->next != nullptr) {
        if(cur->next->val == val) {
            LinkedNode* tmp = cur->next;
            cur->next = cur->next->next;
            delete tmp;
            tmp = nullptr;
        }
        else {
            cur = cur ->next;
        }
    }
    head = dummyHead->next;
    delete dummyHead;
    dummyHead = nullptr;
}
```

A. `dummyHead->next = head; cur = dummyHead;`

B. `dummyHead->next = head->next; cur = dummyHead;`

C. `dummyHead->next = head; cur = dummyHead->next;`

D. `dummyHead->next = head->next; cur = dummyHead->next;`

## Answer

A

## Analysis

使用虚拟头节点（dummyHead）的目的是统一处理删除头节点和其他节点的逻辑。应将 dummyHead 的 next 指向原链表头 head，并将当前指针 cur 初始化为 dummyHead。这样循环中始终通过 cur->next 来判断和操作待删除节点。

## Links

- [[exam-2024-12]]
- [[concept-linked-list]]
